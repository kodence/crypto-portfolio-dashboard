{
  "name": "crypto-portfolio-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local crypto portfolio dashboard with manual price refresh (CoinGecko)",
  "license": "MIT",
  "author": "kodence",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch --env-file-if-exists=.env server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
